rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only allow access through Cloud Functions
    match /{document=**} {
      allow read, write: if false;
    }
  }
}